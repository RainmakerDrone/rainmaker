<h1>Listing votes</h1>

<table>
  <tr>
    <th>Timestamp</th>
    <th>Phone Number</th>
    <th>Vote</th>
  </tr>

<% @votes.each do |vote| %>
  <tr>
    <td><%= vote.created_at %></td>
    <td><%= vote.name %></td>
    <td><%= vote.description.upcase %></td>
  </tr>
<% end %>
</table>

<br />